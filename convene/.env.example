# Port the HTTP/Socket.io server listens on (default: 3000)
PORT=3000

# Optional. When set, Socket.io uses the Redis adapter so rooms work
# across multiple Node instances. Leave unset for single-instance dev.
# REDIS_URL=redis://localhost:6379
